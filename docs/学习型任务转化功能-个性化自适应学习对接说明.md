# 学习型任务转化功能 → 个性化自适应学习对接说明

版本：`1.1`
知识点级交接协议：`learning-task-knowledge-to-personalized-learning-v1`  
纠偏回传协议：`personalized-learning-to-task-conversion-feedback-v1`

## 1. 对接目标

学习型任务页面中的每个知识点都可以直接进入个性化自适应学习。交接时不是把整份任务无差别复制给下游，而是围绕用户点击的知识点，确定性裁剪出：

- 企业任务与学习型任务简介；
- 当前唯一知识点及其学习资源；
- 真正使用该知识点的任务步骤；
- 与这些步骤强相关的技能点；
- 步骤—知识点—技能点特殊关系；
- 下游允许生成和不得改写的字段；
- 理解偏差或弱关系的回传地址。

个性化自适应学习负责“怎么学”，包括学习目标、讲解内容、学习顺序、练习、评价和学习者适配；不得改写企业任务名称、实际任务步骤、阶段产物、检查点或已锁定的强关系。

## 2. 当前实现状态

| 能力 | 状态 |
|---|---|
| 获取整项任务的结构化交付 | 已实现 |
| 按知识点裁剪最小完整交接包 | 已实现 |
| 点击知识点时记录跨功能导航事件 | 已实现 |
| 校验知识点归属和步骤映射 | 已实现 |
| 传递强相关技能及特殊关系 | 已实现 |
| 服务端导入个性化学习项目 | 已实现 |
| 重试时按 `entry_id` 恢复同一项目 | 已实现 |
| 任务、知识点与返回项目身份锁定 | 已实现 |
| 在 LearnFlow 中央工作区展示下游项目 | 已实现 |
| 接收下游纠偏反馈 | 已实现 |
| 反馈持久化与复核追踪 | 已实现 |

前端不需要展示 PDF 或 JSON 下载按钮，所有数据直接通过接口传递。

## 3. 推荐调用流程

```text
用户点击学习型任务中的某个知识点
  → GET 知识点级预览接口
  → 用户确认后 POST 个性化学习启动接口
  → 学习型任务转化功能校验任务、知识点和步骤关系
  → 服务端直接交付 learning-task-knowledge-to-personalized-learning-v1
  → 个性化自适应学习幂等创建或恢复项目
  → LearnFlow 在中央工作区嵌入返回的可信项目地址
  → 如发现关系不密切或范围理解错误，POST 纠偏反馈
  → 学习型任务转化功能形成可追溯复核请求
```

用户确认生成时使用启动接口。它使用服务端保管的配置将交接 JSON 直接导入下游，并记录一次明确的跨功能导航事件：

```text
POST /api/learning-task-conversion/tasks/{task_card_id}/knowledge/{knowledge_id}/personalized-learning-launch
```

只读预取时可使用：

```text
GET /api/learning-task-conversion/tasks/{task_card_id}/knowledge/{knowledge_id}/personalized-learning-entry
```

预览接口返回交接 JSON；启动接口返回 `entry_id`、`project_id`、`knowledge_point_id`、`redirect_url` 和 `created`。启动时会从基础交接 ID 和当前学习者作用域派生不暴露学习者身份的稳定 `entry_id`：同一学习者重试会恢复同一项目，不同学习者不会共用项目。两个接口都复用当前 LearnFlow 登录态。

## 4. 前端调用示例

```ts
export async function launchPersonalizedLearning(
  taskCardId: string,
  knowledgeId: string,
) {
  const response = await fetch(
    `/api/learning-task-conversion/tasks/${encodeURIComponent(taskCardId)}`
      + `/knowledge/${encodeURIComponent(knowledgeId)}/personalized-learning-launch`,
    {
      method: 'POST',
      credentials: 'include',
      headers: { 'Content-Type': 'application/json' },
    },
  )

  if (!response.ok) throw new Error(await response.text())
  const launch = await response.json()
  // redirect_url 已由后端做同源信任校验，页面将它嵌入中央工作区。
  return launch
}
```

## 5. 知识点级交接空模板

该数据由学习型任务转化功能生成，下游只读取，不应自行拼接：

```json
{
  "schema_version": "learning-task-knowledge-to-personalized-learning-v1",
  "entry_id": "",
  "status": "ready",
  "source": {
    "source_system": "learning-work-task-conversion",
    "task_card_id": "",
    "verification_status": ""
  },
  "task_context": {
    "work_task_id": "",
    "enterprise_task_name": "",
    "enterprise_task_description": "",
    "teaching_task_name": "",
    "teaching_task_description": "",
    "work_situation": ""
  },
  "focus": {
    "knowledge_point": {
      "knowledge_id": "",
      "name": "",
      "description": "",
      "learning_resources": []
    },
    "source_steps": [],
    "strongly_related_skills": [],
    "relationships": []
  },
  "generation_contract": {
    "purpose": "围绕选中知识点生成个性化学习目标、内容、练习与评价。",
    "immutable_fields": [],
    "downstream_may_generate": [],
    "must_preserve_relation_traceability": true
  },
  "feedback_contract": {
    "schema_version": "personalized-learning-to-task-conversion-feedback-v1",
    "method": "POST",
    "url": "/api/learning-task-conversion/downstream-feedback",
    "supported_issue_targets": [
      "step_id",
      "knowledge_id",
      "skill_id",
      "relation_id"
    ]
  },
  "navigation": {
    "route_key": "personalized_learning.generate_from_knowledge",
    "entry_path": "",
    "handoff_json_path": "",
    "return_path": ""
  }
}
```

## 6. 知识点级交接完整实例

```json
{
  "schema_version": "learning-task-knowledge-to-personalized-learning-v1",
  "entry_id": "ple_6a79e9f71482dcdf9d7190af",
  "status": "ready",
  "source": {
    "source_system": "learning-work-task-conversion",
    "task_card_id": "ltc_network_vlan_001",
    "verification_status": "verified"
  },
  "task_context": {
    "work_task_id": "wt_network_vlan_001",
    "enterprise_task_name": "交换机 VLAN 配置与连通性验收",
    "enterprise_task_description": "依据网络规划完成 VLAN、端口与链路配置并提交验收记录。",
    "teaching_task_name": "交换机 VLAN 配置与连通性验收学习型工作任务",
    "teaching_task_description": "在校内网络实训环境中完成配置、验证和证据留存。",
    "work_situation": "为新增办公区域完成二层网络隔离和跨交换机 VLAN 连通配置。"
  },
  "focus": {
    "knowledge_point": {
      "knowledge_id": "kp_vlan_8021q",
      "display_code": "K1",
      "name": "VLAN 广播域与 802.1Q 标记",
      "description": "理解 VLAN 对广播域的划分以及 Access、Trunk 端口对帧标签的处理规则。",
      "learning_resources": [
        {
          "resource_id": "res_vlan_official_01",
          "platform": "official_web",
          "resource_name": "交换机 VLAN 配置官方文档",
          "resource_type": "documentation",
          "resource_url": "https://example.edu/vlan-guide",
          "usage": "用于核对端口模式和验收命令"
        }
      ]
    },
    "source_steps": [
      {
        "step": 2,
        "step_id": "step_configure_ports",
        "name": "配置接入与中继端口",
        "action": "按照端口规划配置 Access VLAN 和 Trunk 允许列表，并保存配置。",
        "deliverable": "端口配置记录",
        "check": "端口模式、PVID 和允许 VLAN 与规划表一致。",
        "knowledge_point_ids": ["kp_vlan_8021q"],
        "skill_point_ids": ["sp_vlan_config"]
      }
    ],
    "strongly_related_skills": [
      {
        "skill_id": "sp_vlan_config",
        "display_code": "S1",
        "name": "创建 VLAN 并配置端口模式",
        "observable_action": "能够按规划创建 VLAN，配置 Access 与 Trunk 端口并保存配置。"
      }
    ],
    "relationships": [
      {
        "relation_id": "rel_vlan_config_001",
        "relation_type": "required_for_step",
        "strength": "critical",
        "step_id": "step_configure_ports",
        "knowledge_id": "kp_vlan_8021q",
        "skill_ids": ["sp_vlan_config"],
        "basis": "validated_step_mapping",
        "reason": "802.1Q 标签处理规则直接支撑 Access 与 Trunk 端口配置，并通过配置状态进行验收。"
      }
    ]
  },
  "generation_contract": {
    "purpose": "围绕选中知识点生成个性化学习目标、内容、练习与评价。",
    "immutable_fields": [
      "task_context.work_task_id",
      "task_context.enterprise_task_name",
      "focus.source_steps[].step_id",
      "focus.source_steps[].action",
      "focus.source_steps[].deliverable",
      "focus.source_steps[].check",
      "focus.relationships"
    ],
    "downstream_may_generate": [
      "learning_objectives",
      "learning_content",
      "learning_sequence",
      "practice_activities",
      "assessment_plan",
      "learner_adaptations"
    ],
    "must_preserve_relation_traceability": true
  },
  "feedback_contract": {
    "schema_version": "personalized-learning-to-task-conversion-feedback-v1",
    "method": "POST",
    "url": "/api/learning-task-conversion/downstream-feedback",
    "supported_issue_targets": [
      "step_id",
      "knowledge_id",
      "skill_id",
      "relation_id"
    ]
  },
  "navigation": {
    "route_key": "personalized_learning.generate_from_knowledge",
    "entry_path": "/personalized-learning/tasks/ltc_network_vlan_001/knowledge/kp_vlan_8021q",
    "handoff_json_path": "/api/learning-task-conversion/tasks/ltc_network_vlan_001/knowledge/kp_vlan_8021q/personalized-learning-entry",
    "return_path": "/wf03/tasks/ltc_network_vlan_001"
  }
}
```

示例中的资源网址仅用于说明字段形态，实际使用时必须采用交接数据中已经核验的资源，不能自行拼造链接。

## 7. 下游必须遵守的生成边界

`generation_contract.immutable_fields` 中的字段禁止改写。下游可以生成：

- `learning_objectives`：围绕当前知识点的个性化学习目标；
- `learning_content`：讲解、示例和资源编排；
- `learning_sequence`：依据学习者状态调整的学习顺序；
- `practice_activities`：练习、仿真或实操活动；
- `assessment_plan`：评价方式与达成判断；
- `learner_adaptations`：难度、节奏和支架调整。

下游生成的每项内容应保留以下追溯字段：

```json
{
  "source_entry_id": "ple_...",
  "source_task_card_id": "ltc_...",
  "source_knowledge_id": "kp_...",
  "source_step_ids": ["step_..."],
  "source_relation_ids": ["rel_..."]
}
```

## 8. 纠偏回传接口

如果个性化自适应学习认为某个知识点范围、技能范围或步骤关系不正确，调用交接包 `feedback_contract.url`：

```text
POST /api/learning-task-conversion/downstream-feedback
Content-Type: application/json
```

空模板：

```json
{
  "schema_version": "personalized-learning-to-task-conversion-feedback-v1",
  "task_card_id": "",
  "correlation_id": "",
  "source_system": "个性化自适应学习功能",
  "status": "accepted_with_feedback",
  "issues": [
    {
      "issue_id": "",
      "feedback_code": "step_mapping_mismatch",
      "severity": "warning",
      "relation_id": "",
      "step_id": "",
      "knowledge_id": "",
      "skill_id": "",
      "message": "",
      "suggested_correction": ""
    }
  ],
  "summary": ""
}
```

实例：

```json
{
  "schema_version": "personalized-learning-to-task-conversion-feedback-v1",
  "task_card_id": "ltc_network_vlan_001",
  "correlation_id": "personalized-review-20260817-001",
  "source_system": "个性化自适应学习功能",
  "status": "accepted_with_feedback",
  "issues": [
    {
      "issue_id": "issue_vlan_001",
      "feedback_code": "step_mapping_mismatch",
      "severity": "warning",
      "relation_id": "rel_vlan_config_001",
      "step_id": "step_configure_ports",
      "knowledge_id": "kp_vlan_8021q",
      "skill_id": "sp_vlan_config",
      "message": "该关系能够支撑端口配置，但当前学习内容还需要区分 Access 与 Trunk 两种操作边界。",
      "suggested_correction": "保留原关系，在知识点范围中补充两种端口模式的适用条件和验收差异。"
    }
  ],
  "summary": "任务事实可用，建议补充端口模式的适用边界。"
}
```

`feedback_code` 允许：

- `weak_relation`
- `incorrect_knowledge_scope`
- `incorrect_skill_scope`
- `step_mapping_mismatch`
- `missing_prerequisite`
- `unsupported_task_fact`
- `other`

状态规则：

- `accepted`：不得携带问题；
- `accepted_with_feedback`：必须至少携带一个问题；
- `rejected`：必须至少携带一个问题，并说明拒绝原因。

同一 `task_card_id + correlation_id` 重复提交会更新同一条反馈记录。反馈只进入复核流程，不会直接覆盖企业任务事实；修订后的任务必须重新经过内容与关系门禁。

## 9. 错误处理

| HTTP 状态 | 原因 | 下游处理方式 |
|---:|---|---|
| `401` | 未携带 LearnFlow 登录态 | 使用同源请求并携带 Cookie |
| `404` | 任务或知识点不存在 | 返回任务页并刷新任务包 |
| `422` | 知识点无步骤映射、反馈结构错误 | 阻止生成并展示明确错误 |
| `502`/`504` | 任务服务暂时不可用或超时 | 保留 `entry_id`，允许用户重试 |

不得在接口失败时使用模型猜测任务步骤、知识点或关系作为替代数据。

## 10. 联调验收清单

- [x] 从任务页面点击任意知识点能预览交接，并调用 `POST personalized-learning-launch`。
- [x] 返回的 `knowledge_point_id` 与用户点击项完全一致。
- [x] `source_steps` 至少有一步，并且每条关系都引用已交接的步骤。
- [x] `strongly_related_skills` 只包含当前知识点或来源步骤显式引用的技能点。
- [x] 下游保留 `entry_id`、任务 ID、知识点 ID、步骤 ID 和关系 ID。
- [x] 同一 `entry_id` 重试时恢复同一下游项目。
- [x] 同一任务知识点由不同学习者启动时不会串用下游项目。
- [x] 下游不改写 `generation_contract.immutable_fields`。
- [x] 个性化学习项目保留左侧项目栏与右侧主 Agent，在中央工作区展示。
- [x] 发现偏差时能够在中央工作区成功提交纠偏反馈。
- [x] 用户不需要下载或重新上传 JSON 文件。

## 11. 已有验证位置

- 知识点交接生成：`backend/app/api/learning_task_conversion.py`
- 下游导入与身份校验：`backend/app/services/personalized_learning_handoff.py`
- 前端类型与调用：`frontend/src/services/api.ts`
- 知识点点击入口：`frontend/src/pages/WF03TaskPage.tsx`
- 中央工作区与反馈入口：`frontend/src/pages/PersonalizedLearningEntryPage.tsx`
- 接口测试：`backend/tests/test_learning_task_conversion_api.py`
- 下游对接契约测试：`backend/tests/test_personalized_learning_handoff.py`
