# 学习型任务转化官方插件

该目录定义 LearnFlow 仓库自带的学习型任务转化 Agent Package。插件输出是不可变任务快照，中央工作区由宿主仓库中的官方 React 绑定负责展示；外部 `.lfplugin` 仍只能使用安全的声明式 Surface。

核心边界：

- 输入是已确认的真实工作任务或上游任务 JSON；
- 任务对象、动作、边界和产物不被教学内容替换；
- 输出包含真实作业顺序、步骤产物、验收依据、知识点、技能点和资源；
- 批注形成后继快照，个性化学习只获得知识点级启动包；
- 所有插件事件均为零 Kernel target，不写入五核掌握证据。
