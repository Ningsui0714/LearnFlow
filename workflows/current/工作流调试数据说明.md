# 工作流调试数据索引

每条正式工作流只保留一份本轮交付调试数据，均位于 `debug-data/`，不在本文档中重复粘贴调试输入。

| 工作流文件 | 独立调试数据 |
|---|---|
| `学生画像分析工作流.yml` | `debug-data/学生画像分析工作流.json` |
| `学习阶段个性化讲解工作流_v5.yml` | `debug-data/学习阶段个性化讲解工作流.json` |
| `测验后个性化纠错讲解工作流_v5.yml` | `debug-data/测验后个性化纠错讲解工作流.json` |
| `目标与路径规划工作流.yml` | `debug-data/目标与路径规划工作流.json` |
| `个性化推荐工作流.yml` | `debug-data/个性化推荐工作流.json` |
| `对话问答工作流.yml` | `debug-data/对话问答工作流.json` |
| `测评出题工作流.yml` | `debug-data/测评出题工作流.json` |

调试时在星辰平台选择输入变量 `AGENT_USER_INPUT`，直接复制对应 JSON 文件的完整内容。每个文件本身就是一份可直接粘贴的业务输入，不再包含 `workflow`、`input` 或 `expected_checks` 包装层。

导入后仍需在平台界面将 `appId`、`uid` 与模型配置替换为实际账号，并把发布得到的各 `flow_id` 写入项目环境变量。
